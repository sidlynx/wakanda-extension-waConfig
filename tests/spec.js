/*
describe("Controllers", function () {
    beforeEach(module("waConfig"));
    describe("SectionCtrl", function () {

        var section;
        var $scope = {};
        beforeEach(inject(function ($controller) {
            section = $controller("SectionCtrl", { $scope: $scope });
        }))
        describe("$scope.schema", function () {
            it("check if schema is an object", function () {
                expect(typeof ($scope.section.schema)).toEqual("object");
            })
        })

    })
})


describe("Filters", function () {
    beforeEach(module("waConfig"));
    describe("reverse", function () {
        var reverse;
        beforeEach(inject(function ($filter) {
            reverse = $filter("reverse", {});
        }))

        it("should reverse a string", function () {
            expect(reverse("tahil")).toBe("lihat");
        })
    })
})


//*/

